# Registro de cambios del sistema de instrucciones

Este archivo registra altas, ediciones y bajas de reglas en `.claude/rules/` y en `CLAUDE.md` (raíz). No confundir con el `changelog.md` de cada plantilla individual, que documenta cambios de esa plantilla, no del sistema.

Formato por entrada: `fecha | archivo afectado | qué cambió | por qué`

---

- 2026-07-02 | CLAUDE.md, .claude/rules/*.md (creación inicial) | Migración del framework monolítico original a estructura modular estilo CLAUDE.md | Reducir el archivo raíz a lo estable/siempre-necesario y aislar contenido volátil (matriz de activos), condicional (indicadores nuevos, formato de entrega) y de ejecución (checklist) en archivos separados
- 2026-07-02 | .claude/rules/formato-entrega.md | Añadida restricción dura de compatibilidad con AlgoWizard (apertura/edición sin errores) y protocolo de detección/reporte de conflictos de coherencia | Requisito explícito del usuario: todo `.sqx` generado debe funcionar también en AlgoWizard, y cualquier ajuste que arriesgue la coherencia de capas/versionado/familia debe reportarse al iniciar la revisión, no corregirse en silencio
- 2026-07-02 | .claude/rules/matriz-activo-hipotesis.md | Añadido campo `familia_activo` como metadato de catálogo (no como carpeta) | Decisión de diseño: mantener la organización física simple por Activo, usar la familia solo para redundancia de portafolio y sugerencia de hipótesis análogas
- 2026-07-04 | CLAUDE.md (Paso 8 y prohibiciones), .claude/rules/formato-entrega.md, .claude/rules/checklist-cierre.md | Eliminada la regla dura de compatibilidad con AlgoWizard (Paso 8 completo, dos prohibiciones "nunca", dos ítems de checklist); reemplazada por nota de diseño documentando la decisión | Verificación directa de `internal\ctemplate\wizard.xml` vs `internal\ctemplate\talibs.xml`: AlgoWizard cubre <5% del catálogo de indicadores del Full Builder (7 de ~156) y no soporta el concepto de capas separadas. Mantener la regla habría empobrecido sistemáticamente el diseño de la biblioteca a cambio de un beneficio (edición en AlgoWizard) que este proyecto no necesita. Confirmado explícitamente por el usuario
- 2026-07-05 | .claude/rules/arquitectura-5-capas.md | Reformulada de "estructura que el Builder debe cumplir" a "marco conceptual de diseño"; se aclara que las familias arquitectónicas ya viven en `matriz-activo-hipotesis.md` y que ante conflicto entre capas visibles y amplitud del espacio de búsqueda, gana la amplitud | Verificación directa de `Build-Task1.xml` real (proyecto EURUSD-REVRANGE-M15-001): `RulesComplexity` usa un conteo plano de condiciones, sin ningún concepto nativo de capas separadas en SQX. Se aclara además que esto es independiente de la causa real del fallo de generación de esa plantilla (bloques de tiempo sobrantes activos + rango de Shift mal calibrado, diagnosticado el mismo día) — evita confundir ambos problemas a futuro. Propuesto tras revisión crítica de un prompt externo (ChatGPT) aportado por el usuario; validado contra evidencia propia antes de aceptarlo, no adoptado por autoridad de la fuente
- 2026-07-06 | CLAUDE.md (nuevo Paso 0 y Paso 8), _estado/estado_proyecto.md (creación), .claude/rules/checklist-cierre.md (nuevo ítem) | Se formalizó `SQX_Library` como el proyecto base de desarrollo que se retoma entre sesiones. Se creó `_estado/estado_proyecto.md` como archivo único de continuidad (foco actual, pendientes, historial de sesiones), se añadió su lectura obligatoria como Paso 0 del protocolo y su actualización como Paso 8 de cierre, y un ítem correspondiente en el checklist | Requisito explícito del usuario: que el proyecto siempre sepa "en qué quedamos" sin depender de recordar la conversación puntual. Es un tema genuinamente nuevo (gestión de continuidad entre sesiones), no cubierto por ninguna regla existente
- 2026-07-07 | .claude/rules/calibracion-motor-genetico.md (creación), .claude/rules/configuracion-money-management.md (creación), formato-entrega.md (secciones 12-19 añadidas a la ficha), CLAUDE.md (Paso 6 actualizado), checklist-cierre.md (5 ítems nuevos) | Se diagnosticó con evidencia real la causa de "0 estrategias aceptadas" en `EURUSD-REVRANGE-M15-001`. Se amplió la ficha de diseño para que sea autosuficiente: cubre no solo el diseño conceptual sino toda la configuración del Builder (RulesComplexity, genética, SL/PT, criterios de aceptación por etapa, Trading Options, Money Management, plan de Retest/Optimizer) | Requisito explícito del usuario: poder definir a consideración cada elemento de configuración de la plantilla, aunque en el Full Builder el usuario pueda ajustar sobre la marcha (desviaciones se documentan en el changelog de cada plantilla, mecanismo ya existente). Se investigó primero el motor real (wizard.xml, talibs.xml, conditions.xml, Build-Task1.xml) antes de escribir cualquier regla nueva
- 2026-07-07 | .claude/rules/calibracion-motor-genetico.md, .claude/rules/configuracion-money-management.md, EURUSD/ReversionRango/ficha_diseño.json y changelog.md, _estado/estado_proyecto.md (todos corregidos) | **Corrección de un error propio:** la comparación de configuración registrada más temprano el mismo día usó el `Build-Task1.xml` del proyecto genérico `user/projects/Builder/` (una tarea vieja de "mejora" de una instalación StrategyQuant 4 distinta, no relacionada) en vez del `project.cfx` real de `EURUSD-REVRANGE-M15-001`. Se verificó el archivo correcto: PopulationSize=100/MaxGenerations=50 (no 15/10), Money Management ya era FixedSize/0.1/10000 (no FixedAmount, no había desviación real), 3 condiciones de aceptación en el Build (no 4). Se encontró en cambio un hallazgo nuevo y más relevante: `improveType="strategy"` apunta a una ruta inexistente (`D:\work\StrategyQuant4\...`), pendiente de verificar en la interfaz del Builder | Principio de gobernanza propio (`gobernanza-instrucciones.md`, punto 6: "una regla desactualizada es peor que no tener regla") — se señala y corrige de inmediato en vez de dejar el dato incorrecto documentado
- 2026-07-07 | .claude/rules/calibracion-motor-genetico.md (punto 1 reescrito), formato-entrega.md (sección 15 reescrita), EURUSD/ReversionRango/ficha_diseño.json (sección 15 con justificación por criterio) | Confirmado en la interfaz por el usuario: símbolo/motor corregidos de GBPUSD/MT4 a EURUSD_dukas_M1_UTCPlus02/MetaTrader5 (netted) — verificado por hash y contenido del `project.cfx`. El usuario pidió además que los criterios de aceptación del Build no sean un valor rígido de biblioteca ni se bajen a un filtro trivial (\"no tan basura\"): se analizan con criterio crítico por plantilla (tipo de hipótesis, densidad de oportunidades del timeframe/IS, volatilidad del activo) y se documentan con su justificación, no se copian entre plantillas | Requisito explícito del usuario en dos mensajes seguidos: primero que el Build exija un mínimo de calidad real (no solo ProfitFactor>1 como los ejemplos), después que ese mínimo no sea rígido sino analizado y justificado caso por caso
- 2026-07-07 | .claude/rules/calibracion-motor-genetico.md (punto 2 revertido), formato-entrega.md (sección 0 nueva: resumen ejecutivo obligatorio), checklist-cierre.md (2 ítems nuevos), EURUSD/ReversionRango/ficha_diseño.json (sección 14 corregida, sección 0 añadida) | El usuario corrigió la recomendación de fijar el período de ATR como constante: explorar período y multiplicador de SL/PT como rango permite estructuras de salida dinámicas y es una dimensión de búsqueda legítima, no ruido — se revierte esa parte de la regla. Además pidió que cada ficha incluya una explicación narrativa y profesional (objetivo, oportunidad de mercado, herramientas entregadas y por qué, tipo de resultado buscado) para que cualquiera entienda el diseño sin interpretar campos técnicos sueltos | Requisito explícito del usuario: ser objetivo en el diseño (no fijar por imitar ejemplos sin evidencia propia) y dejar la justificación completa de cada plantilla en un lenguaje claro, no solo en JSON técnico
- 2026-07-07 | user/extend/Snippets/SQ/Blocks/Indicators/ChoppinessIndex/ChoppinessIndex.java (creación + corrección de import/color), user/extend/Snippets/SQ/Blocks/Indicators/EfficiencyRatio/EfficiencyRatio.java (creación), _indicadores/indicadores_propuestos.json (estado → implementado, confirmado) | Confirmado por el usuario: ambos compilan con 0 errores en Code Editor. ChoppinessIndex.java necesitó 2 correcciones reales (import de ChartData debía ser wildcard `com.strategyquant.lib.*`/`datalib.*`/`tradinglib.*` como en ATR.java nativo, no explícito; `Colors.Orange` no existe, se usó `Colors.Red`). Aprendizaje operativo: si el Code Editor tiene el archivo abierto en una pestaña mientras se edita por fuera, al cerrar la app puede sobrescribir la corrección con la versión vieja en memoria — hay que cerrar la pestaña/reabrir el Code Editor antes de recompilar tras una edición externa | El usuario aprobó implementar los 2 indicadores propuestos (Choppiness Index, Kaufman's Efficiency Ratio). Se escribieron en Java siguiendo el patrón exacto de indicadores nativos reales de esta instalación (ATR.java, CCI.java, Highest.java — clases IndicatorBlock, anotaciones @BuildingBlock/@Parameter/@Output/@Indicator/@ParameterSet, método OnBarUpdate). No compilados ni verificados en la aplicación todavía — pendiente que el usuario los abra en Code Editor y confirme que compilan sin errores | Aprobación explícita del usuario ("vale probemos esos indicadores")
- 2026-07-07 | user/projects/EURUSD-REVRANGE-M15-001/project.cfx (editado directamente: Building Blocks de SL/PT restringidos a ATR/MTATR), backup previo guardado, calibracion-motor-genetico.md/ficha_diseño.json/changelog.md actualizados | Tercera corrida real (minConditions=1, 276.993 estrategias) mostró que "cierra en la misma barra" empeoró de 14.79% a 27.27% — se aplicó la restricción de Building Blocks de SL/PT (ya preparada, no aplicada antes) desactivando ~26 mecanismos no-ATR. Aplicado con la aplicación StrategyQuant cerrada (confirmado por el usuario y por log de salida) para evitar el mismo problema de sobreescritura que con Code Editor; verificado por reextracción del zip que símbolo/motor/minConditions no se alteraron | Pedido explícito del usuario ("tienes que arreglar el problema") tras confirmar que el segundo ajuste (minConditions) no fue suficiente por sí solo
- 2026-07-07 | user/projects/EURUSD-REVRANGE-M15-001/project.cfx (corrección: reactivadas 6 referencias básicas de Price Levels), calibracion-motor-genetico.md/ficha_diseño.json/changelog.md actualizados | Al intentar correr el proyecto tras restringir Building Blocks de SL/PT, StrategyQuant devolvió el error "You use Enter at Stop or Limit, you have to choose some Price Level blocks!" — la categoría Price Levels también construye el precio de la orden Limit de entrada, no solo el SL/PT. Se reactivaron Ask/Bid/Open/High/Low/Close (referencias básicas no-indicador), manteniendo apagados los mecanismos basados en indicador (Keltner, Ichimoku, etc.). Nueva regla aprendida documentada: si se restringe Price Levels con entradas Limit/Stop activas, hay que dejar al menos las referencias básicas | Error real devuelto por la aplicación al intentar ejecutar
- 2026-07-07 | .claude/rules/calibracion-motor-genetico.md (hallazgo de testPrecision confirmado con fuente oficial), _estado/estado_proyecto.md | El usuario pidió confirmar por internet si la precisión de datos M1 es suficiente para temporalidades por debajo de H1. Se confirmó con documentación oficial de StrategyQuant (strategyquant.com/doc/strategyquant/data/) que el modo rápido de testeo (testPrecision=1) tiene precisión reconocida como insuficiente "for Stop or Limit orders" — exactamente el caso de esta plantilla (EnterAtLimit, M15, SL/PT por ATR). Se confirmaron los 4 niveles reales de precisión (Selected Timeframe/1 Minute Data/Real Tick Custom Spread/Real Tick Data) y que no hay datos de tick disponibles para EURUSD en esta instalación (M1 es el techo real) | Requisito explícito del usuario: verificar con fuente externa antes de aplicar el ajuste de testPrecision, no asumir solo por comparación con los ejemplos
- 2026-07-07 | user/projects/EURUSD-REVRANGE-M15-001/project.cfx (testPrecision 1→2 en ambos Setups), changelog.md/estado_proyecto.md actualizados | Cuarta corrida real (SL/PT restringido a ATR) dio 0 aceptadas en 3h2min, con "cierra en la misma barra" empeorando a 30.16% — la restricción de Building Blocks no se confirmó como solución. Se aplicó en su lugar el ajuste de testPrecision (1→2), respaldado por documentación oficial de StrategyQuant que reconoce el modo rápido como insuficiente para órdenes Stop/Limit. Aplicado con la app cerrada (confirmado por el usuario), verificado por reextracción del zip | Continuación del plan de diagnóstico ya documentado en calibracion-motor-genetico.md
- 2026-07-07 | .claude/rules/patrones-validados-por-retest.md (corrección de alcance), EURUSD/ReversionRango/ficha_diseño.json (sección 12, referencia retirada) | El usuario aclaró que las 2 estrategias reales analizadas antes (Strategy 1.1.26.sqx, Strategy 4.7.22(1).sqx) no reflejaban una hipótesis intencional — fueron corridas con building blocks abiertos "para que el Builder buscara algún diamante" sin dirección, aportadas solo para aprender la mecánica del archivo, no como evidencia de diseño. Se retiró la conclusión de que "menos condiciones de entrada es mejor diseño" basada en esos 2 casos. Se buscó y confirmó por fuentes independientes (documentación oficial de StrategyQuant + práctica general de trading algorítmico) que el rango minConditions=1/maxConditions=3 sigue siendo razonable, pero por esa vía, no por los 2 ejemplos personales | Corrección explícita del usuario sobre el alcance real de la evidencia aportada; se investigó por fuentes externas en vez de asumir, _indicadores/indicadores_propuestos.json (2 propuestas nuevas: Choppiness Index, Kaufman's Efficiency Ratio) | Se investigó por internet, a pedido del usuario, indicadores no nativos útiles para futuras plantillas. Al buscar cómo instalarlos se encontró que el manual oficial de la instalación (`Extending_SQX_es.pdf`, sección 2.1) contradice lo que decía nuestra regla: los indicadores custom para el Full Builder se programan en **Java** vía Code Editor (Snippets/SQ/Blocks/Indicators), no en MQL4/MQL5 — se corrigió la regla con el proceso real (clases `IndicatorBlock`, anotaciones `@BuildingBlock`/`@Parameter`/`@Output`, métodos `OnBarUpdate`/`OnBlockEvaluate`). Se registraron 2 indicadores investigados (Choppiness Index, Kaufman's Efficiency Ratio) como `propuesto`, con fuente citada, sin implementarlos todavía | Requisito explícito del usuario: usar internet para investigar y documentar indicadores no nativos para plantillas futuras
- 2026-07-07 | .claude/rules/patrones-validados-por-retest.md (creación), arquitectura-5-capas.md (nota de evidencia), CLAUDE.md (Paso 6 referencia nueva), EURUSD/ReversionRango/ficha_diseño.json (sección 18 con pipeline real) | El usuario aportó 2 estrategias EURUSD reales (H1, MT4) que pasaron Retest, ubicadas fuera de la instalación en su carpeta personal (`D:\Ariel De Armas\Forex\EURUSD\ResultadosParciales\`). Se analizó su estructura real: entrada de una sola condición (Bollinger Bands), ejecución con orden Stop, salida por inversión de la propia señal, SL/PT híbrido ATR+Trailing fijo, Money Management FixedSize/0.1 (validación cruzada del estándar ya fijado). Se documentó como evidencia reutilizable y se actualizó el plan de Retest de la ficha con el pipeline real de 4 pruebas (Monte Carlo Manipulation, Monte Carlo Retest, CrossCheck de mayor precisión, perfil de sensibilidad de optimización) | El usuario pidió explícitamente aprender de ejemplos reales propios que pasaron robustez y considerar incorporarlo al proyecto actual con carácter crítico
- 2026-07-07 | user/projects/EURUSD-REVRANGE-M15-001 renombrado a user/projects/EURUSD-REVRANGE-H1-001 (carpeta + config.xml), project.cfx (timeframe H1, SL/PT revertido a valores H1 1.5x-3x/2x-5x, Building Blocks de SL/PT reabiertos, población 100/50), _catalogo/indice_maestro.json (id actualizado, id_anterior conservado), ficha_diseño.json (título, metadata, nota de pivote) | Séptima corrida en M15 (SL/PT ensanchado) confirmó que ese ajuste sí resolvió "cierra en la misma barra" (35%→1.86%) pero disparó rechazo por Profit Factor a 57% — el objetivo de ganancia amplio no encaja con una reversión dentro de rango en M15. Se confirmó que toda la evidencia real disponible (2 estrategias propias + 4 proyectos de ejemplo oficiales) que converge es H1/M30, nunca M15. Se pivotó completo a H1 manteniendo la hipótesis intacta | Decisión conjunta con el usuario: mantener el objetivo de plantillas coherentes pero ganar experiencia real de convergencia en un terreno más favorable antes de volver a acotar hacia M15
- 2026-07-07 | .claude/rules/pipeline-multitarea-y-diseno-is.md (creación), CLAUDE.md (referencia nueva en Paso 6), ficha_diseño.json (sección 19b) | El usuario pidió que cada proyecto tenga todo preconfigurado con sentido común, incluyendo conectar automáticamente Build con el pipeline de Retest/robustez. Se investigó walk-forward analysis (métrica WFE≥0.5), regímenes históricos de EURUSD (2008, 2010-2012, 2016, 2020, 2022) y se verificó contra un proyecto de ejemplo real (GBPJPY BREAKOUT H1) la estructura exacta de encadenado de tareas (config.xml + Build/Retest/ClearDatabanks/GoToTask-TaskN.xml) y del mecanismo nativo Walk-Forward Matrix/Optimization. Se registró como requisito pendiente — se aplica recién cuando una plantilla logre su primera convergencia real, no antes | Pedido explícito del usuario de registrar esto como requisito para cuando pueda validarse
- 2026-07-07 | .claude/rules/proceso-robustez-crosschecks.md (creación), .claude/rules/proceso-portafolio.md (creación), .claude/rules/proceso-exportacion-live.md (creación), CLAUDE.md (referencias nuevas) | El usuario pidió registrar toda la información técnica necesaria para entender coherentemente el proceso completo: pruebas de robustez (Monte Carlo Manipulation/Retest, Retest de mayor precisión, Optimization Profile/SysParam Permutation, Walk-Forward Optimization/Matrix, Sequential Optimization, Retest en mercados adicionales, What-If), construcción de portafolios (Portfolio Composer manual vs. Portfolio Master automático, correlación como criterio central), y exportación a trading en vivo (proceso, y regla operativa de separar generación de ejecución en máquinas distintas). Investigado con documentación oficial de StrategyQuant, con fuentes citadas en cada archivo | Pedido explícito del usuario de documentar cada bloque del proceso (Builder, robustez, Walk-Forward Matrix, portafolios, exportación) para poder ejecutar todo el flujo coherentemente cuando corresponda
- 2026-07-08 | .claude/rules/proceso-exportacion-live.md (sección de validación cruzada MT5 añadida) | El usuario preguntó si es posible probar automáticamente en MT5 una estrategia generada, usando datos disponibles, para comparar contra los resultados de StrategyQuant. Se confirmó que MT5 soporta backtest automatizado por línea de comandos (terminal64.exe /config:ini), pero se verificó que MT5 no está instalado en esta máquina (buscado en C:, D: y todos los perfiles). Se documentó como capacidad pendiente — requiere instalar MT5 y tener al menos una estrategia aceptada real para exportar, cosa que todavía no existe | Pregunta técnica del usuario, investigada y verificada contra el estado real de la instalación antes de responder
- 2026-07-08 | .claude/rules/proceso-exportacion-live.md (sección de migración de datos SQX→MT5) | El usuario preguntó si se pueden migrar los datos de StrategyQuant (limpios/adaptados) a MT5 para las pruebas cruzadas, en vez de depender de datos de un broker demo cualquiera. Se confirmó que existe un proceso oficial documentado por StrategyQuant para esto, usando la herramienta QuantDataManager (exporta a formato HST/FXT nativo de MetaTrader) — verificado que QuantDataManager no está instalado en esta máquina. Se documentó como parte de la capacidad pendiente de validación cruzada MT5 | Pregunta técnica de seguimiento del usuario, investigada y verificada contra el estado real de la instalación
- 2026-07-08 | .claude/rules/vision-proyectos-futuros.md (creación), CLAUDE.md (referencia nueva) | El usuario planteó una visión de largo plazo (EA supervisor de portafolio, retroalimentación con IA para ajustar parámetros en vivo, plataforma web de gestión, estrategia externa vía API de Python). Se registró con evaluación crítica de riesgo por pieza — en particular, se estableció como regla de diseño que cualquier ajuste de parámetros sugerido por análisis de IA sobre datos en vivo debe volver a pasar por el proceso de validación de StrategyQuant antes de aplicarse en una cuenta real, nunca aplicación automática directa. Se fijó un orden de prioridad, ninguna pieza accionable todavía | El usuario pidió mi opinión sobre una visión de proyectos futuros; se documentó para continuidad, no para actuar ahora
- 2026-07-08 | .claude/rules/configuracion-estandar-crosschecks-optimizer.md (creación), .claude/rules/plantillas-cuentas-fondeo.md (creación), CLAUDE.md (referencias nuevas) | El usuario pidió investigar configuraciones comunes de Monte Carlo/SysParam Permutation/Optimizer (con nivel de confianza 95%), y pensar en el diseño futuro de plantillas para cuentas de fondeo (prop firm): reglas distintas (ventanas de tiempo cortas, 1-2 semanas), qué timeframes/criterios de filtrado aplican, análisis por ventanas semanales en vez de años completos, y cuántos intentos paralelos con probabilidad medida hacen falta para pasar ~1 challenge por semana. Se investigó con fuentes externas (reglas típicas de fondeo 2026, tasas de aprobación reales ~5-14%, concepto de "Rolling Window Pass Rate") y se desarrolló razonamiento propio explícito para la metodología de ventanas móviles y la matemática de intentos paralelos (binomial), con advertencia crítica sobre la necesidad de independencia real entre intentos (mismo principio de correlación que en proceso-portafolio.md) — sin eso, correr copias simultáneas de la misma estrategia no son intentos independientes | Pedido explícito del usuario de investigar y razonar sobre esto, con libertad para usar criterio propio donde no hubiera fuente directa
- 2026-07-08 | .claude/rules/criterio-descarte-plantillas.md (creación), CLAUDE.md (referencia nueva) | El usuario preguntó qué pasaría si una plantilla genera estrategias que no pasan Retest/robustez, o que no son tan buenas como se espera — ¿se descartaría el modelo? Se definió el criterio: distinguir "esta instancia falló" (búsqueda insuficiente, calibración pendiente, régimen de IS no representativo) de "la hipótesis no tiene ventaja real" (patrón consistente en múltiples corridas independientes, deterioro sistemático en OOS). Se estableció que nunca se aflojan los criterios de aceptación para forzar un "pase", que los fracasos reales se documentan con el mismo rigor que los éxitos (estado "descartada_no_supera_robustez" en el catálogo, no se borra la entrada), y que "no tan buena como se esperaba" no equivale a "hay que descartarla" si cumple el piso ya justificado | Pedido explícito del usuario de registrar este criterio de decisión como regla formal
- 2026-07-08 | .claude/rules/pipeline-multitarea-y-diseno-is.md (secciones 3b y 3c añadidas) | El usuario pidió investigar todos los tipos de bloques de tarea que existen en un proyecto personalizado de SQ, su función, cómo enlazarlos lógicamente y cómo configurarlos, porque la plantilla requerirá ejecución secuencial tarea a tarea dejando solo los resultados importantes al final. Se enumeraron y verificaron los 21 tipos de tarea reales contra sus archivos task.xml (fuente primaria, no solo documentación): Build, Retest, Optimize, Filtering, CustomAnalysis, CreatePortfolio, AutomaticPortfolioBuilder, ClearDatabanks, GoToTask, StopAndStart, SaveToFiles, LoadFromFiles, DeleteFile, UpdateData, LogDatabankStats, Notification, CallExternalScript, WaitFor, ApplyMassConfig, NeuralNetworkTrainer. Se encontró que "Filtering" y "CustomAnalysis" son exactamente lo necesario para implementar el filtro de Profit Factor OOS discutido antes (evitar candidatas tipo 4.22.136 que pierden en OOS) de forma nativa dentro del pipeline, sin script externo. Se propuso una secuencia concreta completa para la plantilla, todavía no implementada | Pedido explícito del usuario de investigar y documentar esto para poder configurar el pipeline completo cuando corresponda
- 2026-07-09 | .claude/rules/formato-entrega.md (convención de nombres añadida), EURUSD/ReversionRango/candidatas_build/*.sqx (12 archivos renombrados) | El usuario pidió un esquema de nombres para estrategias guardadas similar al de las plantillas (Activo, Temporalidad, Familia de Hipótesis, ID diferenciador) para que el nombre solo ya comunique qué hace la estrategia. Se definió: <ACTIVO>_<TEMPORALIDAD>_<FAMILIAHIPOTESIS>_<ID>.sqx, con el ID combinando ranking+Profit Factor OOS+id interno de SQ. Se renombraron las 12 candidatas ya organizadas (8 activas + 4 descartadas) con el nuevo esquema | Pedido explícito del usuario, registrado como convención formal para toda la biblioteca, no solo esta plantilla
- 2026-07-09 | .claude/rules/proceso-portafolio.md (sección de exportación de portafolio a MT5 añadida) | El usuario preguntó específicamente si Portfolio Master exporta un único EA combinado o EAs separados, cómo maneja el Magic Number por estrategia, y si hay acceso al código .mq5 fuente. Se investigó físicamente en la instalación (proyecto real user/projects/PortfolioMaster/, plugin ResultsSourceCode — el código de generación vive compilado en un .jar, no legible como texto) y se completó con fuentes del foro oficial. Hallazgo clave: no exporta un EA combinado automáticamente (comportamiento por defecto es EAs separados por estrategia), el Magic Number único por estrategia NO es automático (la comunidad tuvo que construir su propia solución), pero sí hay acceso al código .mq5 fuente real | Pregunta técnica específica del usuario, investigada combinando instalación real e internet
- 2026-07-09 | _presentacion/informe_presentacion_v1.html (creación) | El usuario pidió el informe de presentación completo del proyecto (registrado como pendiente en estado_proyecto.md el 2026-07-08), dirigido a público de trading algorítmico no experto en SQ. Cubre: la idea y filosofía, comparación honesta contra otras formas de trabajo, aprendizajes técnicos reales sobre StrategyQuant, cronología honesta de las 13 corridas (incluidos los errores propios), el pipeline de 21 tareas propuesto, capacidades y límites reales de Claude en el proyecto, riesgos de que la hipótesis no tenga ventaja real, visión a futuro con nivel de riesgo por pieza, y una sección explícita de responsabilidad/límites de la IA (portón de validación obligatorio antes de tocar capital real). Guardado dentro del proyecto (_presentacion/) como versión inicial, no solo como Artifact efímero | Pedido explícito del usuario, con instrucción de ser honesto sin dejar nada por decir e incluir el archivo en el proyecto
- 2026-07-09 | _presentacion/informe_presentacion_v1.html (actualizado a v2) | El usuario pidió actualizar el informe con: dato actual de 300 estrategias en el databank, expansión detallada del esquema secuencial totalmente automatizado (cada bloque de SQ prueba algo distinto con filtros diseñados a medida de cada plantilla, no genéricos), reemplazo de todas las menciones de "Claude" por "el asistente de IA" como miembro del equipo, y una nueva sección de valor comercial honesto (fábricas documentadas y con evidencia estadística vs. bots/cajas negras sin respaldo, sin prometer certeza de rendimiento futuro, sin cifra de venta inventada) | Pedido explícito del usuario de actualización del informe ya entregado
- 2026-07-09 | _presentacion/informe_presentacion_v1.html (actualizado a v3) | El usuario pidió una portada con banner animado en JavaScript ("Grupo de Desarrollo de Software y Trading Algorítmico"), una imagen relacionada con "motor de hipótesis entrando a una fábrica de bots ganadores" generada por una IA especializada en imagen, y navegación lateral con enlace a cada título. Se verificó honestamente (ToolSearch) que no hay ninguna herramienta de generación de imágenes disponible en este entorno — no se fabricó ninguna API usada. En su lugar se construyó un gráfico generativo real vía Canvas/JavaScript (motor pulsante con anillos, partículas fluyendo hacia una grilla de "fábrica" que se ilumina), más un banner de texto animado por JavaScript (efecto máquina de escribir) y una navegación lateral fija con scroll-spy a las 11 secciones, respetando prefers-reduced-motion | Pedido explícito del usuario; se corrigió honestamente la premisa de "usar una IA de imagen" al no existir esa herramienta disponible
- 2026-07-11 | .claude/rules/vision-proyectos-futuros.md (sección 1 marcada como movida fuera del proyecto), .claude/rules/integracion-gestorportafolio.md (creación) | El usuario informó que el "EA supervisor de portafolio" ya no se desarrolla dentro de SQX_Library — se está construyendo por otra vía como proyecto independiente ("GestorPortafolio"). Se retiró esa pieza de la visión de este proyecto (mantenida solo como nota histórica) y se creó un archivo nuevo con la especificación técnica completa de integración: bots exportados deben incluir PortfolioParticipant.mqh y una condición cooperativa de pausa (!IsBotPausedByPortfolioManager) SOLO en la rama de apertura de nueva operación, nunca en gestión de posición abierta. Se revisó críticamente antes de entregarla: se confirmó con documentación oficial de MQL5 que la lectura frecuente de la variable global evita su expiración a 4 semanas, se sugirió GlobalVariablesFlush() para robustez ante crashes, y se dejó constancia honesta de que SQX_Library todavía no exportó ningún bot real a .mq5, así que la estructura exacta del código generado no está verificada de primera mano | Pedido explícito del usuario de dejar de rastrear el bot supervisor como visión propia y preparar la información para pasarla a otra IA
- 2026-07-11 | .claude/rules/pipeline-multitarea-y-diseno-is.md (sección 3d añadida) | El usuario pidió que se fuera preparando cómo se van a agregar los nuevos bloques de trabajo al proyecto, pensando en sus configuraciones y por qué, dado que pronto habrá suficiente materia prima (candidatas) para completar la plantilla. Se investigó a fondo el mecanismo real de CustomAnalysis (ejemplo nativo OutlierTrade.java): es Java, mismo patrón que los indicadores custom, con acceso directo a SampleTypes.OutOfSample y StatsKey.PROFIT_FACTOR vía el motor de estadísticas real de StrategyQuant — hallazgo importante: no hace falta seguir calculando el Profit Factor OOS a mano con sqcli+awk, existe la función nativa exacta. Se dejó un borrador concreto (FilterByOOSProfitFactor.java, no escrito/compilado todavía) y propuestas de configuración concretas para Filtering, Retest, CreatePortfolio/AutomaticPortfolioBuilder, SaveToFiles, LogDatabankStats, Notification, ClearDatabanks y GoToTask, basadas en evidencia real de esta instalación | Pedido explícito del usuario de ir aprendiendo y preparando las configuraciones antes de tener la materia prima lista
- 2026-07-11 | .claude/rules/pipeline-multitarea-y-diseno-is.md (sección 3e añadida) | El usuario planteó el plan operativo para el momento de ensamblar el pipeline: guardar las 1000 estrategias a salvo cuando termine la corrida (para no depender de re-correr el Build si algo sale mal), y poder "desmarcar" la tarea de Build de la secuencia mientras se desarrollan/prueban las tareas nuevas. Se confirmó que el mecanismo ya existe nativamente: cada <Task> en config.xml tiene un atributo active="true/false" (verificado en el proyecto propio y en el ejemplo real de GBPJPY) — no hace falta nada nuevo, solo poner active="false" en el Build mientras se prueba el resto y reactivarlo después | Pedido explícito del usuario de recordar este plan operativo antes de ensamblar el pipeline
- 2026-07-11 | _indicadores/indicadores_propuestos.json (15 indicadores nuevos + 2 patrones de velas añadidos) | El usuario pidió investigar 10-20 indicadores novedosos con respaldo real, y algunos patrones de velas novedosos, no incluidos en SQX ni en el proyecto, aplicables tanto a forex como a índices. Se verificó primero el catálogo nativo completo (talibs.xml crudo + building blocks curados) para no duplicar. Se investigaron y documentaron con fuente: Fisher Transform, Connors RSI, Relative Vigor Index, Relative Volatility Index (Dorsey), Coppock Curve, KST, FRAMA, Chandelier Exit, Mass Index, Klinger Volume Oscillator, Ease of Movement, Elder's Force Index, Twiggs Money Flow, Hurst Exponent, y half-life de reversión a la media (Ornstein-Uhlenbeck) — este último y el Hurst Exponent conectan directamente con la calibración de la hipótesis de reversión en rango ya en desarrollo. Para patrones de velas se encontraron solo 2 genuinamente distintos de los 61 nativos de TA-Lib (que ya cubren el canon clásico japonés): Pin Bar e Inside Bar (price-action moderno). Todos marcados como "propuesto", ninguno implementado todavía | Pedido explícito del usuario, investigado mientras la corrida del Build seguía en curso
- 2026-07-11 | 17 archivos .java nuevos en user/extend/Snippets/SQ/Blocks/ (15 en Indicators/, 2 pares en CandlePatterns/), _indicadores/indicadores_propuestos.json (17 entradas actualizadas a "implementado_pendiente_verificar") | El usuario pidió ejecutar la implementación completa, revisión crítica y compilación de los 15 indicadores + 2 patrones de vela propuestos, dejando la respuesta de SQ (compilación real en Code Editor) para después. Se escribió cada archivo siguiendo patrones nativos verificados (IndicatorBlock con Chart.X.get(idx) para indicadores, ConditionBlock con Chart.X(shift) para patrones de vela, calculadores nativos reutilizados: AverageCalculator SMA/EMA/LWMA, HighestCalculator, LowestCalculator, RSICalculator, StdDevCalculator, SumCalculator). Se hizo revisión crítica propia: balance de llaves verificado en los 19 archivos, nombre de clase = nombre de archivo verificado en los 19, sintaxis de @Parameter/@BuildingBlock/@Indicator contrastada contra 6+ ejemplos nativos reales (incluida la corrección de quitar un @Indicator() vacío en FRAMA.java que no coincidía con el patrón real de KAMA.java). Klinger Volume Oscillator se implementó con una simplificación documentada explícitamente (la fórmula original de "cm" acumulado varía entre fuentes secundarias). NO se compiló en Code Editor — StrategyQuant tenía un Build activo corriendo y no se quiso interferir, queda pendiente para cuando el usuario decida abrir la app | Pedido explícito del usuario, con instrucción de dejar la verificación con SQ para después
- 2026-07-11 | 18 archivos .java nuevos en user/extend/Snippets/SQ/Blocks/ (16 en Indicators/, 2 en CandlePatterns/ para divergencia), _indicadores/indicadores_propuestos.json (17 entradas nuevas: 15 indicadores + 1 par de patrones de divergencia, más los que ya estaban) | El usuario pidió una segunda ronda exhaustiva y sin límite de cantidad, esta vez orientada a otras familias de hipótesis que se desarrollarán después (Ruptura/Breakout, Tendencia/Momentum, Expansión de volatilidad, Gap de apertura, Divergencia), no solo reversión. Se investigaron y verificaron contra el catálogo nativo completo: Donchian Channel, GMMA, Trend Intensity Index, tres filtros de Ehlers (Super Smoother, Roofing Filter, Instantaneous Trendline, con fórmulas exactas verificadas), Bollinger Band Width, Bollinger %B, TTM Squeeze (el más complejo: combina Bollinger+Keltner+regresión lineal), Historical Volatility, Chaikin Volatility, VWAP Bands, Anchored VWAP (con regla de anclaje automática diseñada por el proyecto, ya que el anclaje manual clásico no es automatizable), Gap Index (diseño propio del proyecto, sin autor externo), Z-Score Precio vs Media, Percentage Volume Oscillator, y un detector de Divergencia RSI/Precio (ConditionBlock que compara swing points, con RSI simplificado documentado explícitamente). Revisión crítica igual de rigurosa que la primera ronda: 22 archivos nuevos, balance de llaves 100% correcto, nombre de clase = nombre de archivo 100% correcto, sintaxis verificada contra ejemplos nativos (incluida la corrección de un helper de ChaikinVolatility que reconstruía la EMA de forma aproximada e ineficiente, reemplazado por un buffer circular exacto). Tampoco compilado en Code Editor, mismo motivo (Build activo) | Pedido explícito del usuario, con permisos ampliados para ejecutar la tarea sin pausas, sin límite de cantidad, priorizando hallazgos con peso real y novedad genuina
- 2026-07-12 | user/projects/EURUSD-REVRANGE-H1-001 (Build finalizado, 1000 estrategias), Reversion-Media/_backup_1000_bruto_2026-07-12/ (backup externo), EURUSD/ReversionRango/changelog.md, _estado/estado_proyecto.md (actualizados) | La duodécima corrida (testPrecision=1) terminó por `StopCondition databank-full` al llegar a 1000 estrategias (2 días 16 hrs). Las 1000 fueron exportadas por el usuario vía interfaz y movidas por el asistente a una subcarpeta de backup dedicada, separada de la carpeta reservada para candidatas curadas finales | Pedido explícito del usuario de resguardar las 1000 antes de tocar el pipeline nuevo
- 2026-07-12 | .claude/rules/trazabilidad-versionado-estrategias.md (creación), CLAUDE.md (referencia nueva en Paso 6) | El usuario planteó que las estrategias pasarán por Walk-Forward Matrix, Optimize y formarán parte de portafolios desplegados en real, y preguntó si el mecanismo de trazabilidad debía construirse a nivel de código fuente exportado o de archivo nativo `.sqx`. Se investigó contra el proyecto de ejemplo real `Optimizer/` (estrategias reales que pasaron Optimize y WFM): el ID interno de SQ (`gen.individuo.variante`) no cambia entre etapas, solo WFM antepone un prefijo de nombre distintivo, Optimize no agrega ningún marcador y usa un sufijo `(N)` puramente anticolisión sin significado semántico (confirmado con un caso real de colisión), y no hay ningún campo de linaje dentro del `.sqx` (comparado byte a byte el árbol de reglas entre versión original y WFM: idéntico). Conclusión: SQ no da trazabilidad nativa en ningún nivel — se diseñó un mecanismo propio (tags de etapa en el nombre de archivo + registro central `linaje_estrategias.json` por hipótesis) | Pedido explícito del usuario de investigar antes de decidir el mecanismo
- 2026-07-12 | .claude/rules/pipeline-multitarea-y-diseno-is.md (sección 3f añadida) | El usuario recordó que, al terminar la plantilla, el proyecto se guardará fuera de `user/projects/` (carpeta por defecto de StrategyQuant, donde conviven los proyectos de ejemplo de la instalación) para no mezclarlo con esos — confirmó por prueba propia que SQ permite abrir un proyecto desde cualquier ubicación vía un cuadro de diálogo de "buscar proyecto custom", no limitado a `user/projects/`. Registrado como plan para cuando el pipeline esté terminado; ubicación destino exacta todavía sin definir | Pedido explícito del usuario de dejarlo anotado para no perderlo
- 2026-07-12 | Repositorio Git creado (`SQX_Library/.git`, remoto privado `github.com/adearmasfinanzas-a11y/SQX-library`), `.gitignore` añadido, `_indicadores/codigo_fuente_java/` creado (copia de respaldo de los 39 archivos Java, que por requisito de la instalación viven en `user/extend/Snippets/`, fuera del árbol versionado) | El usuario preguntó si había copia de seguridad del proyecto en la nube por si hay un problema con la computadora — no la había (`D:\StrategyQuantoInstalado` no era un repo Git). Se creó el repo local, se conectó a un repo privado nuevo bajo la cuenta de GitHub del usuario (distinta de la del proyecto GestorPortafolio), se autenticó con un Personal Access Token de alcance limitado (guardado de forma segura en Windows Credential Manager, no en texto plano), y se hizo el primer commit (81 archivos) | Pedido explícito del usuario
- 2026-07-12 | user/extend/Snippets/SQ/Blocks/ (6 archivos corregidos: CoppockCurve, KST, FRAMA, RelativeVigorIndex, HurstExponent, BearishRSIDivergence, BullishRSIDivergence — 7 archivos en 6 indicadores/patrones), _indicadores/indicadores_propuestos.json (36 entradas actualizadas de "implementado_pendiente_verificar" a "implementado" con confirmación de compilación) | El usuario compiló los 39 archivos Java en Code Editor. Primer intento: 2 errores reales en `CoppockCurve.java` (método privado `rocAt()` no declaraba `throws TradingException` pese a llamar a `Chart.Close.get()`, que sí la lanza). Se corrigió y se encontró el mismo bug preventivamente en 5 archivos más antes de que fallaran. **Incidente operativo:** la primera corrección de `CoppockCurve.java` fue revertida por la propia aplicación al cerrarla (tenía una pestaña abierta con la versión vieja en memoria desde el primer intento fallido) — mismo mecanismo ya documentado en un indicador anterior (2026-07-07). Se corrigió una segunda vez con el Code Editor cerrado, sin abrir la pestaña del archivo, y compiló limpio: "Compilación de fragmentos correcta, 0 error(s)" sobre los 39 archivos. Actualización del JSON hecha vía PowerShell `ConvertFrom-Json`/`ConvertTo-Json` (no hay Python en el entorno) y **verificada campo por campo contra la versión committeada en git** (no solo por diff de texto, que resultaba engañoso por el reformateo completo que aplica `ConvertTo-Json`) para descartar pérdida de datos — confirmado 0 discrepancias en los 37 registros | Confirmado por el usuario en Code Editor
- 2026-07-12 | .claude/rules/pipeline-multitarea-y-diseno-is.md (sección 3d corregida: se retira `FilterByOOSProfitFactor.java`) | **Corrección de un error propio:** el usuario preguntó por qué haría falta escribir código Java para el filtro de Profit Factor OOS si la tarea se arma desde la interfaz — pregunta que reveló que la propuesta de `CustomAnalysis` en Java (registrada el 2026-07-11) era una sobre-complicación innecesaria. Se decompiló la clase real `com.strategyquant.tradinglib.SampleTypes` con `javap` (incluido en `j64/bin/` de la propia instalación) y se confirmó `OutOfSample = 20`, ya usado en `sampleType="20"` en archivos XML nativos reales (`internal/plugins/TaskRetest/task.xml`). La tarea nativa `Filtering` usa la misma estructura genérica de condiciones (`<Column-Value>` con selector de tipo de muestra) que `Build`/`Retest`, por lo que puede filtrar por Profit Factor Out-of-Sample directamente desde la interfaz, sin ningún código propio | Pregunta crítica del usuario que expuso una sobre-ingeniería propia; corregido con evidencia real antes de escribir el archivo (que nunca se llegó a crear)
- 2026-07-12 | .claude/rules/mecanismo-condiciones-filtrado.md (creación), CLAUDE.md (referencia nueva) | El usuario pidió aprender a fondo todas las capacidades de la tarea `Filtering` (y del framework de condiciones que comparte con Build/Retest) que pudieran servir como criterio en otras plantillas futuras, no solo la actual. Se decompilaron con `javap` (incluido en la instalación) las clases reales del motor: catálogo completo de ~70 columnas estadísticas (`StatsKey`), tabla completa de tipos de muestra con sus valores numéricos reales (`SampleTypes` — incluye corridas individuales de Walk-Forward `OutOfSample1-10`/`InSampleValidation1-10`), `Directions`, `PlTypes`, y el mecanismo interno de `FilteringTask` (`actionType`: Deleted/Copied/Moved; `maxStrategies`; `conditionsType` con dos modos especiales adicionales para reutilizar el motivo de rechazo del Build). Se documentó honestamente qué quedó sin verificar (atributos `market`/`subresult`/`confidenceLevel`, probablemente ligados a CrossCheck/Monte Carlo, no confirmados) | Pedido explícito del usuario de documentar esto como conocimiento reutilizable, no solo para el pipeline actual
- 2026-07-12 | user/projects/EURUSD-REVRANGE-H1-001 (primera tarea `Filter Strategies` armada en la interfaz, databank nuevo `OOS_Filtrado`), EURUSD/ReversionRango/pipeline_tareas/filtering_task1_condiciones_oos.xml (creación, copia del XML real exportado), mecanismo-condiciones-filtrado.md (3 correcciones en vivo + confirmación de `market`/`subresult`/`confidenceLevel`) | Se armó junto con el usuario, paso a paso desde la interfaz, la primera tarea del pipeline nuevo: `Filter Strategies`, acción `Moved` (Results → `OOS_Filtrado`, databank creado previamente a mano, el campo Target no admite nombre libre), sin tope de cantidad ("todas", el `maxStrategies` es independiente de las condiciones — corrección del usuario), modo "aplica a las que cumplan las condiciones" (`ConditionsType=Keep`). Condiciones definidas con criterio propio (no copiadas): ProfitFactor OOS ≥1.2 (más estricto que el piso de 1.0 propuesto antes, justificado por tener ahora 1000 en bruto en vez de 12) y NumberOfTrades OOS ≥30 (evita que un PF OOS alto se apoye en pocas operaciones), ambas en dirección "Both" y unidad "Money" (coherencia con cómo el propio Build evalúa sus condiciones, y con el estándar FixedSize de la biblioteca). Guardada la configuración a XML real (`D:\Ariel De Armas\Templates SQX\filters.xml`), copiada al proyecto para registro, y usada para confirmar con evidencia real (no solo decompilación) los atributos que quedaban pendientes de verificar en `mecanismo-condiciones-filtrado.md` | Trabajo conjunto con el usuario, tres correcciones suyas en el camino (acción no es "borrar" sino elegir entre copiar/mover; el campo de cantidad no es el filtro sino un tope aparte; el databank destino se crea antes en la herramienta de Databanks, no se escribe libre)
- 2026-07-12 | .claude/rules/mecanismo-condiciones-filtrado.md (corrección de `ConditionsType`), EURUSD/ReversionRango/pipeline_tareas/Filtro_OOS_PF1.2_Trades30.cfx (archivado) | **Corrección de un error propio:** se había documentado, basándose solo en el nombre del campo `Keep=1` de la clase decompilada `ConditionsTypes`, que ese valor correspondería a "aplica a las estrategias que cumplan las condiciones". El usuario guardó el preset completo de la tarea (botón "Guardar configuración", `.cfx` real) y el `config.xml` interno mostró `ConditionsType=0` para esa misma opción seleccionada — el nombre del enum no coincidía con el comportamiento real. Corregido con la evidencia del preset guardado, no con la decompilación sola. Se confirmaron además `ActionType=2`=Moved y `MaxStrategies=0`="todas" (sin tope), y se identificó que el archivo trae un bloque `<Rankings>` residual heredado del proyecto que no es configuración propia de Filtering | Principio de gobernanza propio: nunca confiar en el nombre de un campo/clase sin confirmar contra un archivo real exportado por la aplicación
- 2026-07-12 | .claude/rules/pipeline-multitarea-y-diseno-is.md (secuencia 3c actualizada: `SaveToFiles` agregado justo después del Build) | El usuario preguntó si en plantillas futuras haría falta guardar el resultado completo del Build o solo lo que pasa el primer filtro (por el backup manual de las 1000 que se hizo esta vez, al ser la primera plantilla en converger). Se estableció con razonamiento: el Build no es reproducible determinísticamente (búsqueda genética con aleatoriedad, dos corridas con la misma config dan resultados distintos) mientras que Filtering sí lo es (mismas condiciones + mismo databank = mismo resultado siempre) — por eso el resultado bruto del Build es irrecuperable si se pierde, y el de Filtering es barato de reconstruir. Como el pipeline hace loop de vuelta al Build, cada vuelta genera un lote distinto — se fijó `SaveToFiles` del resultado completo del Build como paso automático estándar en cada vuelta del loop para toda plantilla futura, no como precaución manual puntual | Pregunta de diseño del usuario, razonada con la asimetría de costo/reproducibilidad entre Build y Filtering ya documentada
- 2026-07-12 | .claude/rules/pipeline-multitarea-y-diseno-is.md (secciones 3g y 3h nuevas, secuencia 3c actualizada, corrección de "el pipeline hace loop") | Consolidación de varias decisiones de diseño discutidas con el usuario en la misma sesión: (1) el usuario aclaró que el pipeline no hace loop automático por defecto — puede ser una corrida completa única, repetida solo si el usuario lo decide explícitamente, no un `GoToTask` desatendido; (2) se definió dónde viven físicamente los resultados: los `.sqx` (binarios, no diffeables) van a `Reversion-Media\_runs\<fecha>_corridaN\`, fuera de la instalación de SQ, con backup manual a otro disco por el usuario — nunca a git; `SQX_Library` se queda con reglas/changelog/catálogo/presets/registro de metadatos (`linaje_estrategias.json`), y no hace falta moverla de dentro de la instalación porque ya tiene su propio respaldo real vía el repo Git de GitHub creado hoy; (3) se aclaró con criterio propio que la tarea `CreatePortfolio`/`AutomaticPortfolioBuilder` dentro del pipeline de una sola plantilla no es el portafolio final de despliegue (todas las estrategias comparten hipótesis/activo/timeframe, poco probable que tengan baja correlación real entre sí) — es un paso de "de-duplicación de la plantilla", el portafolio real se arma después combinando varias plantillas distintas vía PortfolioMaster/PortfolioComposer | Serie de preguntas críticas del usuario sobre organización de archivos y arquitectura del pipeline, resueltas en conjunto antes de seguir armando tareas
- 2026-07-12 | .claude/rules/mecanismo-savetofiles.md (creación), CLAUDE.md (referencia nueva), EURUSD/ReversionRango/pipeline_tareas/SaveToFiles_BrutoBuild.cfx (archivado) | Se armó junto con el usuario la tarea `SaveToFiles` para el resultado en bruto del Build (1000 estrategias) — Input `Results`, formato SQX activado, exportación de resumen de databank activada (barato, un solo archivo), exportación de trades/HTML/PDF/código fuente desactivadas (caras y prematuras para material sin filtrar), nota custom `corrida01_bruto_build_2026-07-12`. Se decompiló `SaveToFiles.class` y se confirmó con el preset real exportado: 7 tipos de exportación independientes, catálogo real de formatos de código fuente (incluye Expert Advisor for MetaTrader5 *.MQ5 — hallazgo relevante para `proceso-exportacion-live.md`), Magic Number nativo (corrige lo documentado antes en `proceso-portafolio.md` sobre que no era automático), etiquetado nativo con nota custom. Se encontró y documentó un bug real de la interfaz: nombres de preset con un segmento puramente numérico aislado entre guiones bajos (`_01_`) son rechazados como "nombre de archivo no válido" — quitar el segmento numérico aislado lo resuelve | Trabajo conjunto con el usuario, siguiendo la metodología ya establecida de investigar antes de proponer
- 2026-07-12 | .claude/rules/pipeline-multitarea-y-diseno-is.md (secciones 1b nueva, Retest actualizada con investigación real) | El usuario reveló que actualizó los datos históricos de la instalación hasta 2026 pero deliberadamente no los incluyó en el rango de fechas del Build (que se detuvo en 2024.12.31) — separación física, no solo partición estadística, para que el Retest tenga datos genuinamente nunca vistos por el Build. Se investigó la tarea `Retest` (comparado `task.xml` por defecto contra `Retest-Task2.xml` real de `GBPJPY BREAKOUT H1`: Databank Input=Output, Money Management FixedSize por defecto, Walk-Forward Optimization/Matrix presente en la estructura pero desactivado incluso en el proyecto real). Se buscó en internet si la práctica de reservar datos físicamente fuera del alcance del Build es reconocida: confirmado, es una versión reforzada de walk-forward validation con "embargo period" y la técnica de "purging" contra data leakage — se fija como práctica estándar para toda plantilla futura, con fuentes citadas | Pedido explícito del usuario de verificar la práctica con fuentes externas antes de darla por buena
- 2026-07-12 | .claude/rules/pipeline-multitarea-y-diseno-is.md (sección 1c nueva) | El usuario pidió formalizar que, para toda plantilla futura, el asistente pueda leer los datos disponibles del activo, contrastarlos con información de internet (regímenes de mercado reales del activo concreto), y sugerir automáticamente el rango de tiempo del Build, la división IS/OOS y el criterio de distribución de esos períodos según el histórico real y la hipótesis. Se documentó honestamente que el punto de "leer datos disponibles" todavía no tiene método confirmado (se intentó leer el `.dat` binario directamente en esta sesión y no fue posible sin herramientas propias de SQ) — queda como investigación pendiente, por ahora se le pide al usuario que confirme el rango real. La actualización de datos en sí se sigue haciendo manual por la interfaz, con la tarea nativa `UpdateData` como candidata a automatizar más adelante | Pedido explícito del usuario, registrado con honestidad sobre la capacidad real actual vs. la deseada
- 2026-07-12 | .claude/rules/pipeline-multitarea-y-diseno-is.md (sección 1c ampliada) | El usuario pidió verificar si las herramientas de SQ permiten leer/actualizar datos sin la interfaz. Se encontró que SQ corre un servidor HTTP local propio (`localhost:8080`, confirmado con el proyecto abierto) con una API REST interna (`/data/checkQualitySummary`, `/data/updateAll`, etc., identificadas en el JS del frontend). No se probó en la práctica: las llamadas POST reales requieren cuerpo comprimido en gzip con codificación particular y posible token de sesión — se decidió no arriesgar una llamada mal armada contra el proyecto real en uso. Queda documentado como vía de investigación confirmada pero pendiente, a desarrollar en un contexto sin riesgo (app cerrada o proyecto de prueba) | Pedido explícito del usuario de verificar antes de descartar la posibilidad; se priorizó la seguridad del proyecto real en uso sobre completar la investigación de inmediato
- 2026-07-12 | .claude/rules/pipeline-multitarea-y-diseno-is.md (catálogo de tareas, entrada Retest ampliada + AutomaticRetest agregada) | El usuario notó que en la interfaz existen dos tareas distintas, `AutomaticRetest` y `RetestStrategies`, no catalogadas como diferentes antes. Se decompilaron ambos plugins: `Retest` es una única configuración fija de datos/época; `AutomaticRetest` (`XmlChartCombinator`, `AutomaticRetestTask`) genera el producto cartesiano de combinaciones de símbolo/fecha/spread y las corre en lote vía `GridClient` — pensado para estudios de sensibilidad a gran escala, no para una revalidación puntual. Se confirmó que `Retest` es la tarea correcta para revalidar contra la ventana OOS reservada; `AutomaticRetest` queda anotada como candidata para más adelante | Pregunta del usuario que expuso un hueco real en el catálogo de tareas ya documentado
- 2026-07-12 | .claude/rules/pipeline-multitarea-y-diseno-is.md (sección 1b ampliada) | Al armar el Setup del Retest, el usuario configuró `dateFrom` igual al `dateFrom` original del Build (no arrancando en la ventana reservada) y `dateTo` hasta el dato más reciente (2026.04.16), explicando su razonamiento de coherencia. Se analizó y confirmó como correcto por dos motivos: warmup de indicadores de período largo (el `ReservedBars` nativo, default 50, no alcanza) y para poder comparar en un solo reporte el desempeño en el período ya conocido por el Build junto con el tramo genuinamente nuevo. Se fija como regla: el `dateFrom` del Retest siempre hereda el del Build, nunca arranca en la ventana reservada; la distribución IS/OOS para el reporte de estadísticas es un paso aparte (pestaña de Test Parameters) | Pedido explícito del usuario de entender y registrar el detalle para poder aplicarlo de forma automática en plantillas futuras
- 2026-07-12 | CLAUDE.md (Paso 4 ampliado), .claude/rules/configuracion-money-management.md (sección nueva sobre comisiones/spread) | Se detectó que la tarea Retest tenía "No commissions | No swap" mientras el Build real sí usaba costos (`Commission SizeBased=6`, `Swap money long=-6.5/short=-3.5`) — discrepancia real de coherencia. El usuario pidió, además de corregirlo, que para toda plantilla futura se defina el broker objetivo en conversación previa con el usuario ANTES de configurar el Build (nunca un valor genérico sin preguntar), y propuso el patrón de usar comisión/spread genéricos pero justificados en el Build, subiéndolos moderadamente (~30-35%) en el Retest como prueba de margen de seguridad — no al revés. Se registró como regla dura en el Paso 4 del protocolo | Pedido explícito del usuario, con ejemplo numérico concreto aplicado a esta plantilla
- 2026-07-12 | .claude/rules/pipeline-multitarea-y-diseno-is.md (sección Retest consolidada con la pestaña "Data" completa) | Se armó junto con el usuario, campo por campo y con capturas reales, la pestaña "Data" completa de la tarea Retest: symbol/timeframe/engine idénticos al Build, dateFrom heredado del Build, dateTo hasta el dato más reciente, precisión "1 minute data tick simulation", comisión/swap subidos ~30-35% (verificado con captura del panel real), slippage=1 pip (valor moderado, no porcentual porque el Build usa 0), min.distance=0 (no es costo a estresar, es restricción estructural del broker, se mantiene igual al Build). Se documentó que los 5 presets nativos de "Data range parts" (distribución IS/ISV/OOS) no sirven porque son splits por porcentaje y nuestro límite OOS es una fecha exacta — se usa "Add new part" para armarlo a mano | Pedido explícito del usuario de consolidar el aprendizaje de esta pestaña para futuras intervenciones automáticas
- 2026-07-12 | .claude/rules/pipeline-multitarea-y-diseno-is.md (hallazgo de bug documentado en sección Retest), .claude/rules/checklist-cierre.md (ítem nuevo) | Al configurar "Trading options" del Retest, se encontró un bug real de conversión de unidades en el Build ya corrido: `FridayCloseTime=2300`/`SundayOpenTime=2300` (para `DontTradeOnWeekends=true`) se guardan en segundos desde medianoche (confirmado con otros dos campos que sí convirtieron exacto: `83040s→23:04`, `74400s→20:40`), pero `2300` interpretado como segundos da `00:38`, no `23:00` — valor sin sentido práctico, probablemente por escribir el número de la hora crudo en vez de convertirlo a segundos. `DontTradeOnWeekends=true` estuvo activo con este valor roto durante toda la corrida real del Build (2.5 días, 1000 estrategias); en la práctica el efecto fue casi nulo. No se rehace el Build ya corrido. Corregido en el Retest (`23:00` real vía la interfaz) y agregado como ítem de verificación al checklist de cierre para toda plantilla futura | Hallazgo real durante la configuración conjunta con el usuario, verificado con capturas reales antes de asumir que era un bug (se descartó primero un error propio de conversión antes de concluir que el bug estaba en el dato guardado)
- 2026-07-12 | .claude/rules/pipeline-multitarea-y-diseno-is.md (ATM, Money management y Cross checks del Retest documentados) | Confirmado ATM `enable=false` (coincide con el Build, nunca usado) y Money Management `FixedSize`/0.1/10000 (sin cambios) para el Retest. Se revisó la pantalla real de "Cross checks (robustness)" (9 pruebas agrupadas en Basic/Standard/Extensive) y se encontraron dos configuraciones heredadas sin sentido real: "Higher backtest precision" activado a un nivel que ya es igual al de la precisión principal (redundante, se apaga), y "Backtests on additional markets" apuntando a una variante de datos del mismo EURUSD en vez de un mercado distinto (se cambia a GBPUSD). Se acordó con el usuario dividir la robustez en 3 pasadas separadas de Retest: esta primera (solo esos dos ajustes), una segunda con Monte Carlo, y una tercera con Walk-Forward Matrix (preferido sobre Walk-Forward Optimization) | Decisión conjunta con el usuario para no pagar el costo computacional completo de robustez sobre candidatas que todavía no pasaron los filtros previos
- 2026-07-12 | .claude/rules/pipeline-multitarea-y-diseno-is.md (aclaraciones sobre "Backtests on additional markets" y herencia de configuración entre tareas) | El usuario notó que "Backtests on additional markets" tiene su propio Setup independiente (fechas/spread/comisión propios de GBPUSD, no heredados de la pestaña Data del Retest principal) y preguntó si las próximas tareas Retest (pasadas de Monte Carlo/Walk-Forward Matrix) vendrían precargadas con la configuración de este Retest o habría que redefinir todo. Sin evidencia directa confirmada todavía, se identificó una pista fuerte en el menú contextual ya visto ("Copiar la configuración de la tarea") que sugiere que la herencia NO es automática — se recomendó usar ese mecanismo manual en vez de rearmar desde cero, pendiente de confirmar en la práctica | Pregunta del usuario que expuso dos huecos reales en la documentación del pipeline
- 2026-07-12 | .claude/rules/pipeline-multitarea-y-diseno-is.md (Setup de GBPUSD en "Backtests on additional markets" documentado con valores confirmados) | Se configuró y verificó con capturas reales el cross-check de mercado adicional (GBPUSD): patrón de toggle individual por campo confirmado, spread 0.5 y comisión $3/lote round-trip verificados con fuente externa (FTMO), diferencia real de disponibilidad de histórico entre EURUSD y GBPUSD detectada (`2026.04.16` vs `2026.04.10`, no se fuerza a coincidir), decisión de usar costos reales sin estrés adicional en este cross-check (a diferencia del Retest principal). Swap se dejó con el valor real que ya traía por defecto, sin tocar | Trabajo conjunto con el usuario, con verificación externa de cada valor antes de aceptarlo
- 2026-07-13 | .claude/rules/pipeline-multitarea-y-diseno-is.md (pestaña Ranking del Retest documentada completa) | Se armó "Data range parts" con dos correcciones en el camino (primer intento mezclaba el OOS interno del Build con la ventana nueva; segundo intento dejó un tramo mal ubicado que en realidad era In-Sample) hasta llegar a la config correcta: un único tramo OOS `2025.01.01`-`2026.04.13`. El usuario pidió explícitamente no asumir que el combo de filtros por defecto (Trades/mes, PF, Ret/DD) era suficiente — se buscó en internet y se encontró que faltaba una dimensión (consistencia ajustada por riesgo), agregándose `Sharpe Ratio (OOS) > 0.75` con fuente externa citada. Se interpretó y decidió el toggle "Run crosschecks independently from Custom filters failure" (apagado, para no correr cross-checks caros sobre candidatas que ya van a descartarse) | Pedido explícito del usuario de investigar en vez de asumir que los valores por defecto son siempre los adecuados
- 2026-07-13 | EURUSD/ReversionRango/pipeline_tareas/Retest_strategies_task1.cfx (archivado), pipeline-multitarea-y-diseno-is.md (verificación final con el preset real) | El usuario guardó y compartió el preset completo de la tarea Retest ya armada. Se verificó campo por campo contra el `.cfx` real: todo coincidió con lo acordado (fechas, precisión, comisiones/swap de ambos mercados, las 4 condiciones de Ranking con sus sampleType exactos). Se confirmó el nombre real del campo XML detrás de "Run crosschecks independently..." (`ForceRunCrossChecks=false`) y se verificó matemáticamente que el fix del bug de fin de semana quedó correcto (82800 segundos = 23:00 exacto) | Cierre de verificación de la tarea Retest completa, mismo rigor que las anteriores
- 2026-07-13 | .claude/rules/pipeline-multitarea-y-diseno-is.md (umbral de Ret/DD Ratio corregido de `>4` a `>=3`) | El usuario notó que muchas candidatas quedaban agrupadas entre 3 y 4 en el filtro `Ret/DD Ratio (OOS)` y preguntó si el umbral debía bajarse. Se razonó que `4` era más estricto que el propio criterio ya validado del Build (`Ret/DD>3` sobre ~10 años), pero esta condición corre sobre una ventana OOS mucho más corta (~15 meses) con más ruido estadístico — exigir un umbral más duro sobre una muestra más chica no selecciona necesariamente mejores estrategias. Se bajó a `>=3`, igualando el criterio ya validado del Build en vez de superarlo, dado que `Sharpe Ratio` ya cubre la selectividad adicional de esta etapa | Pregunta crítica del usuario basada en observación directa de los datos, no solo en la fórmula teórica
